# kind cluster for the observability lab.
# 1 control-plane + 2 workers gives enough room to spread the observability
# stack and the app services across nodes. Port maps expose Grafana and the
# gateway on the host for convenience.
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: obs-lab
nodes:
  - role: control-plane
    kubeadmConfigPatches:
      - |
        kind: InitConfiguration
        nodeRegistration:
          kubeletExtraArgs:
            node-labels: "ingress-ready=true"
    # Host ports are offset (18080/13000) so the cluster can run alongside the
    # local docker-compose dev stack, which already uses 8080/3000.
    extraPortMappings:
      - containerPort: 30080   # gateway NodePort
        hostPort: 18080
        protocol: TCP
      - containerPort: 30030   # Grafana NodePort
        hostPort: 13000
        protocol: TCP
  - role: worker
  - role: worker
